# What the Space installs before Gradio, pinned to the versions the app was built and tested with.
# PyMuPDF is left out because the Space never reads the PDFs, only the index built from them.
# CPU-only torch: the default Linux wheel brings ~2.5 GB of CUDA libraries a CPU Space can't use.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.7.1
sentence-transformers==6.0.1
transformers==5.17.0
tokenizers==0.23.2
huggingface_hub==1.31.0
faiss-cpu==1.15.1
numpy==2.2.6
rank-bm25==0.2.2
snowballstemmer==3.1.1
rapidfuzz==3.14.6
openai==1.82.0
python-dotenv==1.0.1
certifi
fastapi==0.136.3
starlette==1.2.1
pydantic==2.11.5
uvicorn[standard]==0.49.0
